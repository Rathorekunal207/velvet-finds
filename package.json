{
  "name": "velvet-finds",
  "version": "2.0.0",
  "description": "Velvet FINDS — Curated Affiliate Product Discovery Site with Amazon India Integration",
  "private": true,
  "scripts": {
    "dev": "netlify dev",
    "build": "echo 'Static site — no build step required.'",
    "start": "netlify dev"
  },
  "dependencies": {},
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  },
  "engines": {
    "node": ">=18"
  }
}
